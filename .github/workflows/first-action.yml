name: Manual-Job
'on':
  workflow_dispatch:
      
jobs:
  APi-Tests:
    runs-on: [self-hosted]
    steps:
      - name: Run Tests
        run: |
          echo  ${GITHUB_API_URL}
          echo "Hello ${qarp_attributes} "
        shell: bash
